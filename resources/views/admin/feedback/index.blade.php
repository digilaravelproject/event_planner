@extends('admin.layout')
@section('content')<div class="admin-page">@include('admin.partials.module-header',['title'=>'User Feedback','subtitle'=>'Review ratings, resolve user concerns and keep an auditable response history.'])@include('admin.partials.alerts')
<div class="mb-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Average Rating',$stats['average'].' / 5'],['Total Feedback',$stats['total']],['Pending Feedback',$stats['pending']],['Resolved Feedback',$stats['resolved']]] as [$label,$value])<div class="admin-card p-5"><p class="text-xs font-bold uppercase tracking-wide text-slate-400">{{ $label }}</p><p class="mt-2 text-2xl font-extrabold text-slate-800">{{ $value }}</p></div>@endforeach</div>
<form class="admin-card mb-5 grid gap-3 p-4 md:grid-cols-5"><input name="search" value="{{ request('search') }}" placeholder="Search feedback..." class="admin-control px-4 py-2.5 text-sm md:col-span-2"><select name="status" class="admin-control px-3 py-2.5 text-sm"><option value="">All statuses</option>@foreach($statuses as $option)<option value="{{ $option->value }}" @selected(request('status')===$option->value)>{{ $option->label }}</option>@endforeach</select><select name="sort" class="admin-control px-3 py-2.5 text-sm"><option value="created_at">Newest</option><option value="rating" @selected(request('sort')==='rating')>Rating</option><option value="subject" @selected(request('sort')==='subject')>Subject</option></select><button class="admin-primary-button rounded-xl text-sm font-bold text-white">Filter</button></form>
<div class="admin-card overflow-hidden"><div class="overflow-x-auto"><table class="w-full"><thead><tr><th class="px-5 py-4 text-left">User</th><th class="px-5 py-4 text-left">Subject</th><th class="px-5 py-4 text-left">Rating</th><th class="px-5 py-4 text-left">Status</th><th class="px-5 py-4 text-left">Created</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody>@forelse($feedbackItems as $feedback)<tr class="border-t"><td class="px-5 py-4"><p class="text-sm font-bold">{{ $feedback->user?->name??'Deleted user' }}</p><p class="text-xs text-slate-400">{{ $feedback->user?->email??'—' }}</p></td><td class="px-5 py-4"><p class="max-w-xs truncate text-sm">{{ $feedback->subject }}</p></td><td class="px-5 py-4 text-sm font-bold text-amber-500">{{ str_repeat('★',$feedback->rating) }}<span class="text-slate-300">{{ str_repeat('★',5-$feedback->rating) }}</span></td><td class="px-5 py-4">@include('admin.partials.status-badge',['status'=>$feedback->status])</td><td class="px-5 py-4 text-xs">{{ $feedback->created_at->format('d M Y') }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.feedback.show',$feedback) }}" class="rounded-lg bg-indigo-50 px-3 py-1.5 text-xs font-bold text-indigo-700">View</a><form method="POST" action="{{ route('admin.feedback.destroy',$feedback) }}" data-confirm="Delete this feedback?">@csrf @method('DELETE')<button class="rounded-lg bg-rose-50 px-3 py-1.5 text-xs font-bold text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="p-16 text-center text-sm text-slate-400">No feedback found.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $feedbackItems->links() }}</div></div></div>@endsection
