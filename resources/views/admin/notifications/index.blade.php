@extends('admin.layout')
@section('content')<div class="admin-page">@include('admin.partials.module-header',['title'=>'Notification Management','subtitle'=>'Create, schedule, send and audit user notifications.','actionUrl'=>route('admin.notifications.create'),'actionLabel'=>'Create Notification'])@include('admin.partials.alerts')
<form class="admin-card mb-5 grid gap-3 p-4 md:grid-cols-5"><input name="search" value="{{ request('search') }}" placeholder="Search notifications..." class="admin-control px-4 py-2.5 text-sm md:col-span-2"><select name="status" class="admin-control px-3 py-2.5 text-sm"><option value="">All statuses</option>@foreach(['draft','scheduled','sent'] as $value)<option value="{{ $value }}" @selected(request('status')===$value)>{{ ucfirst($value) }}</option>@endforeach</select><select name="sort" class="admin-control px-3 py-2.5 text-sm"><option value="created_at">Newest</option><option value="title" @selected(request('sort')==='title')>Title</option><option value="schedule_at" @selected(request('sort')==='schedule_at')>Schedule</option></select><button class="admin-primary-button rounded-xl text-sm font-bold text-white">Filter</button></form>
<div class="admin-card overflow-hidden"><div class="overflow-x-auto"><table class="w-full"><thead><tr><th class="px-5 py-4 text-left">Notification</th><th class="px-5 py-4 text-left">Type</th><th class="px-5 py-4 text-left">Recipients</th><th class="px-5 py-4 text-left">Schedule</th><th class="px-5 py-4 text-left">Status</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody>@forelse($notifications as $notification)<tr class="border-t"><td class="px-5 py-4"><p class="text-sm font-bold text-slate-800">{{ $notification->title }}</p><p class="max-w-xs truncate text-xs text-slate-400">{{ $notification->message }}</p></td><td class="px-5 py-4 text-sm capitalize">{{ str_replace('_',' ',$notification->notification_type) }}</td><td class="px-5 py-4 text-sm font-bold">{{ $notification->users_count }}</td><td class="px-5 py-4 text-xs">{{ $notification->schedule_at?->format('d M Y, h:i A')??'Immediate' }}</td><td class="px-5 py-4">@include('admin.partials.status-badge',['status'=>$notification->status])</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.notifications.show',$notification) }}" class="rounded-lg border px-3 py-1.5 text-xs font-bold">View</a><a href="{{ route('admin.notifications.edit',$notification) }}" class="rounded-lg bg-indigo-50 px-3 py-1.5 text-xs font-bold text-indigo-700">Edit</a><form method="POST" action="{{ route('admin.notifications.destroy',$notification) }}" data-confirm="Delete this notification and its history?">@csrf @method('DELETE')<button class="rounded-lg bg-rose-50 px-3 py-1.5 text-xs font-bold text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-16 text-center text-sm text-slate-400">No notifications found.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $notifications->links() }}</div></div></div>@endsection
