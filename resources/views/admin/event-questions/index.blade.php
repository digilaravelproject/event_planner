@extends('admin.layout')
@section('content')<div class="admin-page">@include('admin.partials.module-header',['title'=>'Event Requirement Questions','subtitle'=>'Control the ordered questions shown by the frontend AI planner.','actionUrl'=>route('admin.event-questions.create'),'actionLabel'=>'Add Question'])@include('admin.partials.alerts')
<form class="admin-card mb-5 grid gap-3 p-4 md:grid-cols-5"><input name="search" value="{{ request('search') }}" placeholder="Search questions..." class="admin-control px-4 py-2.5 text-sm md:col-span-2"><select name="status" class="admin-control px-3 py-2.5 text-sm"><option value="">All statuses</option><option value="1" @selected(request('status')==='1')>Enabled</option><option value="0" @selected(request('status')==='0')>Disabled</option></select><select name="sort" class="admin-control px-3 py-2.5 text-sm"><option value="display_order">Display order</option><option value="question" @selected(request('sort')==='question')>Question</option><option value="created_at" @selected(request('sort')==='created_at')>Newest</option></select><button class="admin-primary-button rounded-xl text-sm font-bold text-white">Filter</button></form>
<div class="admin-card overflow-hidden"><div class="overflow-x-auto"><table class="w-full"><thead><tr><th class="px-5 py-4 text-left">Order</th><th class="px-5 py-4 text-left">Question</th><th class="px-5 py-4 text-left">Type</th><th class="px-5 py-4 text-left">Required</th><th class="px-5 py-4 text-left">Status</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody>@forelse($questions as $question)<tr class="border-t border-slate-100"><td class="px-5 py-4"><span class="rounded-lg bg-slate-100 px-2 py-1 text-xs font-bold">{{ $question->display_order }}</span></td><td class="px-5 py-4"><p class="text-sm font-bold text-slate-800">{{ $question->question }}</p><p class="font-mono text-[11px] text-slate-400">{{ $question->question_code }}</p></td><td class="px-5 py-4 text-sm capitalize">{{ $question->question_type }}</td><td class="px-5 py-4 text-sm">{{ $question->is_required?'Yes':'No' }}</td><td class="px-5 py-4">@include('admin.partials.status-badge',['status'=>$question->status])</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.event-questions.edit',$question) }}" class="rounded-lg bg-indigo-50 px-3 py-1.5 text-xs font-bold text-indigo-700">Edit</a><form method="POST" action="{{ route('admin.event-questions.destroy',$question) }}" data-confirm="Delete this question?">@csrf @method('DELETE')<button class="rounded-lg bg-rose-50 px-3 py-1.5 text-xs font-bold text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-16 text-center text-sm text-slate-400">No questions found.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $questions->links() }}</div></div></div>@endsection
