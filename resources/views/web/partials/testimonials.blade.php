<div id="testimonials" class="relative z-10 px-4 pb-12 sm:px-8 md:px-12 md:pb-16 lg:px-16" x-data="{ currentIndex: 0, itemsPerPage: 4, next(){this.currentIndex=(this.currentIndex+1)%Math.max(1,{{ $items->count() }}-this.itemsPerPage+1)}, prev(){this.currentIndex=Math.max(0,this.currentIndex-1)} }">
    <div class="relative z-10 mx-auto max-w-[1600px] space-y-6">
        <div class="flex flex-col items-center justify-between gap-4 border-b border-rose-100/80 pb-4 sm:flex-row"><div><span class="rounded-full border border-rose-200/50 bg-rose-100/60 px-2.5 py-0.5 text-[10px] font-bold uppercase text-[#850625]">Love & Stories ({{ $items->count() }} Reviews)</span><h2 class="mt-2 font-serif-luxury text-xl font-extrabold text-slate-900 md:text-3xl">What Our Couples & Families Say</h2></div><div class="flex gap-2"><button @click="prev()" class="h-8 w-8 rounded-full border border-rose-200 bg-white">‹</button><button @click="next()" class="h-8 w-8 rounded-full border border-rose-200 bg-white">›</button></div></div>
        <div class="overflow-hidden"><div class="flex -mx-2 transition-transform duration-500" :style="'transform:translateX(-'+(currentIndex*25)+'%)'">
            @foreach($items as $item)<div class="w-full shrink-0 px-2 sm:w-1/2 lg:w-1/3 xl:w-1/4"><article class="flex h-full flex-col justify-between rounded-xl border border-rose-100/90 bg-white p-5 shadow-sm"><div><div class="flex items-center gap-3">@if($item->image)<img src="{{ str_starts_with($item->image,'http') ? $item->image : asset('storage/'.$item->image) }}" alt="{{ $item->title }}" class="h-10 w-10 rounded-full object-cover">@endif<div><h4 class="font-serif-luxury text-xs font-bold text-slate-900">{{ $item->title }}</h4><p class="text-[10px] text-slate-500">{{ $item->subtitle }}</p></div></div><div class="my-3 text-[10px] text-[#D4AF37]">{{ str_repeat('★',(int)data_get($item->meta,'rating',5)) }}</div><p class="text-xs italic leading-relaxed text-slate-600">“{{ $item->body }}”</p></div><div class="mt-4 flex items-center justify-between border-t border-slate-100 pt-3"><span class="text-[10px] font-bold text-emerald-700">{{ data_get($item->meta,'footer') }}</span><span class="text-[10px] text-slate-400">{{ data_get($item->meta,'date') }}</span></div></article></div>@endforeach
        </div></div>
    </div>
</div>
