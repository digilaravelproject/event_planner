@extends('vendor.layout')
@section('title', 'My Businesses') @section('page-title', 'My Businesses')
@section('content')
<section class="admin-hero flex flex-col gap-5 p-7 text-white sm:flex-row sm:items-center sm:justify-between"><div class="relative z-10"><p class="text-xs font-bold uppercase tracking-[.2em] text-amber-200">Your catalogue</p><h2 class="mt-2 text-3xl font-extrabold">Business & vendor details</h2><p class="mt-2 text-sm text-blue-100">Manage only the listings created under your account.</p></div><a href="{{ route('vendor.vendors.create') }}" class="relative z-10 rounded-xl bg-white px-5 py-3 text-sm font-extrabold text-[#30448b]">+ Add New Listing</a></section>
<form method="GET" class="admin-card mt-6 grid gap-4 p-5 sm:grid-cols-[1fr_180px_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search your listings..." class="rounded-xl border-slate-200 px-4 py-3 text-sm"><select name="status" class="rounded-xl border-slate-200 px-4 py-3 text-sm"><option value="">All statuses</option>@foreach(['active','inactive','draft','archived'] as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-5 py-3 text-sm font-bold text-white">Filter</button></form>
<section class="admin-card mt-6 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left"><thead class="bg-slate-50 text-[10px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Business</th><th class="px-5 py-4">Category</th><th class="px-5 py-4">Status</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">
@forelse($vendors as $vendor)<tr><td class="px-5 py-4"><a href="{{ route('vendor.vendors.show',$vendor) }}" class="text-sm font-extrabold text-slate-900 hover:text-[#3950a2]">{{ $vendor->name }}</a><p class="mt-1 text-[11px] text-slate-400">Updated {{ $vendor->updated_at->diffForHumans() }}</p></td><td class="px-5 py-4 text-sm text-slate-600">{{ $vendor->category }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-3 py-1 text-[10px] font-extrabold uppercase text-slate-600">{{ $vendor->status }}</span></td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('vendor.vendors.show',$vendor) }}" class="rounded-lg border border-blue-200 bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700">View</a><a href="{{ route('vendor.vendors.edit',$vendor) }}" class="rounded-lg border border-violet-200 bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">Edit</a><form method="POST" action="{{ route('vendor.vendors.destroy',$vendor) }}" onsubmit="return confirm('Delete this listing and its history?')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-xs font-bold text-rose-700">Delete</button></form></div></td></tr>
@empty<tr><td colspan="4" class="p-12 text-center text-sm text-slate-500">No business listings found.</td></tr>@endforelse
</tbody></table></div>@if($vendors->hasPages())<div class="border-t border-slate-100 p-5">{{ $vendors->links() }}</div>@endif</section>
@endsection
